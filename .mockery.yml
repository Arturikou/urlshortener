with-expecter: true
all: true

packages:
  github.com/Arturikou/urlshortener/internal/handlers:
    config:
      dir: "mocks"
      outpkg: "mocks"
      filename: "{{.InterfaceName | snakecase}}_mock.go"
    interfaces:
      URLService: {}
      Repo: {}